version: '3.8'

services:
  Comcast-Backend-Dev:
    image: 'comcast-backend-dev:${TAG-latest}'
    build:
      dockerfile: .dockerfile
    ports:
      - "8082:8000"
#    env_file:
#      - .env
    volumes:
      - ./app:/app/app
#    depends_on:
#      - db
